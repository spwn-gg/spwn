#!/usr/bin/env bash
# A helper invoked by session-created.sh — shows that the single hook file can
# orchestrate other scripts/code. Put real setup steps here (install deps, etc.).
set -euo pipefail
echo "  [setup/install] (put real setup here, e.g. npm install)"
