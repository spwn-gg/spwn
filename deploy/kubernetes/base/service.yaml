apiVersion: v1
kind: Service
metadata:
  name: spwn
  labels:
    app.kubernetes.io/name: spwn
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: spwn
  ports:
    - name: http
      port: 80
      targetPort: http
