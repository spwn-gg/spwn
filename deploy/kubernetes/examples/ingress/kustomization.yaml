# spwn at the root of its own hostname, through any Ingress controller.
#
#   kubectl apply -k deploy/kubernetes/examples/ingress
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: spwn
resources:
  - namespace.yaml
  - ../../base
  - ingress.yaml
