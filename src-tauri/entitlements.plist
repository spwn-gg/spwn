<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The bundled `node` sidecar is V8/JIT. Under the hardened runtime the Tauri
       bundler applies, V8 cannot reserve executable memory for its CodeRange
       without these, and node aborts at startup ("Failed to reserve virtual
       memory for CodeRange"), so the chat sidecar never streams. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
